{
  "name": "live-video-clip-cutter",
  "version": "1.0.0",
  "description": "A web app for cutting live video clips",
  "dependencies": {
    "express": "^4.17.1", // For your server
    "ffmpeg-static": "^4.4.0", // For FFmpeg operations
    "formidable": "^1.2.2", // For handling form data
    "socket.io": "^4.1.3", // For real-time communication (optional)
    "axios": "^0.24.0", // For making HTTP requests (optional)
    "vue": "^2.6.14", // For building the frontend (optional)
    "nuxt": "^2.15.8" // For server-rendered Vue.js (optional)
  },
  "devDependencies": {
    // Add your development dependencies here
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate"
    // Add more scripts as needed
  }
}
